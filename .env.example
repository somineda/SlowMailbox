# Database Configuration (TortoiseORM uses postgres:// for asyncpg)
# For local PostgreSQL without password:
DATABASE_URL=postgres://your_username@localhost:5432/slowmail
# Or with password:
# DATABASE_URL=postgres://user:password@localhost:5432/slowmail

# Gmail SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=fuurele@gmail.com
SMTP_PASSWORD=rkam bbyu qpsr cidy

# Application Settings
DELAY_DAYS=7
